#include <stdio.h>

/**
 * main - Print the alphabet in lowercase, followed by a new line
 * return: Always 0
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
